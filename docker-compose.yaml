services:
  evoapi:
    container_name: evoapi
    image: evoapicloud/evolution-api:v2.3.7
    restart: always
    volumes:
      - evolution_instances:/evolution/instances
    networks:
      - traefik_network
    env_file:
      - .env

volumes:
  evolution_instances:
    driver: local

networks:
  traefik_network:
    external: true
